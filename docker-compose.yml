services:
  core-terminal:
    platform: linux/amd64
    image: crivero1/cer-core
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    ports:
      - 3002:3002
